---
import {
  getLocaleFromPath,
  getPrefix,
  localeMap,
  type LocalesValues,
} from "intlayer";

import Cfp from "../../../components/2027/cfp/index.astro";

export const getStaticPaths = () =>
  localeMap(({ locale }) => ({
    params: { locale: getPrefix(locale).localePrefix },
  }));

const locale = getLocaleFromPath(Astro.url.pathname) as LocalesValues;
---

<Cfp locale={locale} />
