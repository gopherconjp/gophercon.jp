---
import { getIntlayer, type LocalesValues } from "intlayer";

import Breakable from "../../_common/breakable/index.astro";
import ContentBox from "../../_common/content-box/index.astro";

interface Props {
  locale: LocalesValues;
}
const { locale } = Astro.props;

const { title, heading, body } = getIntlayer("2027-about-section3", locale);
---

<section class="about-section3">
  <ContentBox variant="text">
    <div class="about-section3__content">
      <h2 class="about-section3__title">
        <Breakable value={title} locale={locale} />
      </h2>
      <h3 class="about-section3__heading center">
        <Breakable value={heading} locale={locale} />
      </h3>
      <p class="about-section3__body">
        <Breakable value={body} locale={locale} />
      </p>
    </div>
  </ContentBox>
</section>

<style>
  .about-section3__content {
    display: grid;
    gap: var(--_spacing-32);
    padding: var(--_spacing-64) 0;
    text-align: center;
  }

  .about-section3__title,
  .about-section3__heading,
  .about-section3__body {
    margin: 0;
  }

  .about-section3__title {
    width: fit-content;
    max-width: 100%;
    justify-self: center;
    text-align: left;
  }
</style>
