---
interface Props {
  level: 1 | 2 | 3;
  text: string;
  center?: boolean;
}
const { level, text, center = false } = Astro.props;
---

{level === 1 && <h1>{text}</h1>}
{level === 2 && <h2>{text}</h2>}
{level === 3 && <h3 class:list={{ center }}>{text}</h3>}
