name: Release

on:
  workflow_dispatch:

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  tag:
    name: Tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: production
    permissions:
      contents: write
    outputs:
      tag: ${{ steps.tag.outputs.tag }}
    steps:
      - name: Checkout main
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: main
          fetch-depth: 0
          persist-credentials: false
      - name: Create tag
        id: tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          REMOTE_URL="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"

          PREFIX=$(TZ=Asia/Tokyo date +'%Y.%m%d')
          MAX=$(git ls-remote --tags "${REMOTE_URL}" "refs/tags/${PREFIX}.*" | awk -F. '$NF ~ /^[0-9]+$/ {print $NF}' | sort -n | tail -1)
          TAG="${PREFIX}.$(( ${MAX:--1} + 1 ))"

          git tag "${TAG}" "$(git rev-parse HEAD)"
          git push "${REMOTE_URL}" "${TAG}"

          echo "tag=${TAG}" >> "${GITHUB_OUTPUT}"

  release:
    name: Release
    needs: tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    environment: production
    permissions:
      contents: write
    steps:
      - name: Generate release notes
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          TAG: ${{ needs.tag.outputs.tag }}
        with:
          script: |
            await github.rest.repos.createRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag_name: process.env.TAG,
              generate_release_notes: true,
            });

  deploy:
    name: Deploy
    needs: tag
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.tag.outputs.tag }}
          persist-credentials: false
      - name: Setup Bun environment
        uses: gopherconjp/actions-toolkit/setup-bun@24ab3555c6584a02083c51b14ce53b8e481006dd # main
      - name: Deploy to Cloudflare Workers
        run: bun run deploy
        env:
          SITE_URL: https://gophercon.jp
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
