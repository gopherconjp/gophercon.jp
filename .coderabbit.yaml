language: "ja-JP"
reviews:
  # Request changes to block merging when critical issues are found
  request_changes_workflow: true
  auto_review:
    enabled: true
    drafts: false
    ignore_usernames:
      - "renovate[bot]"
